## API de desenvolvimento WEB (não gostei muito...)
$ pip install streamlit

## ORM para mexer com banco de dados relacional (simples de usar, mas tomar cuidado com a implementação para não 
##      gerar entradas que possibilitam SQL injection!)
$ pipinstall SQLAlchemy

## API para comunicação com apenas o BD postgreSQL
pip install psycopg2

## API para mexer com mongoDB
pip install pymongo

## API para mexer com requisições http async (sem sincronicidade)
pip install aiohttp

## API para manipulação de dados em planilhas com Python
pip install openpyxl