DEBUG = True

# Dados da conexão com o banco
USERNAME = ''
# Se tiver @ na senha é necessario fazer r'senha%40' em vez de 'senha@'
PASSWORD = ''
SERVER = 'localhost'
DB = 'api_flask'

# URI de conexão com o banco
SQLALCHEMY_DATABASE_URI = f'mysql+pymysql://{USERNAME}:{PASSWORD}@{SERVER}/{DB}'

# Para utilizar o migration
SQLALCHEMY_TRACK_MODIFICATIONS = True

# Chave para o flash message e jwt
SECRET_KEY="chave_secreta"

# Configuração de cookies para autenticação da api e do frontend
# colocando o local onde os tokens serão armazenados
JWT_TOKEN_LOCATION = ["cookies"]
# colocando o nome que serão armazenados para busca
JWT_ACCESS_COOKIE_NAME = "access_token_cookie"
# Removendo o CSRF_PROTECT para funcionar sem passar o token CSRF pois sem javascript 
# não é possivel passar no formulário, o que torna o jinja inútil ao fazer
JWT_COOKIE_CSRF_PROTECT = False